import { atom } from 'recoil';

export const PointHistroyIdAtom = atom({
  key: 'PointHistoryId',
  default: '',
});

export const DeleteTagIdAtom = atom({
  key: 'DeleteTagIdAtom',
  default: '',
});
